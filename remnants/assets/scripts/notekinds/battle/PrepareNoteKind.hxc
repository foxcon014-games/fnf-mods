package remnants.notekinds.battle;

import funkin.play.notes.notekind.NoteKind;

class PrepareNoteKind extends NoteKind
{
  public function new()
  {
    super('battle-prepare-remnants', 'Prepare (DadBattle Remnants)');
    this.noanim = true;
  }
}
